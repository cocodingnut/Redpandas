import { HideTimerDirective } from './hide-timer.directive';

describe('HideTimerDirective', () => {
  it('should create an instance', () => {
    const directive = new HideTimerDirective();
    expect(directive).toBeTruthy();
  });
});
